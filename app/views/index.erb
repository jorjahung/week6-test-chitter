<div id="peeps-container">
<ul id='peeps'>
	<% @peeps.sort_by{ |peep| peep.created_at}.reverse.each do |peep| %>
		<%= partial :peep, :locals => {:peep => peep, :user => @users}  %>
	<% end %>
</ul>
</div>


